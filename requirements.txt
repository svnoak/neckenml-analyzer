# Python dependencies for neckenml-analyzer testing framework
# Install with: pip install -r requirements.txt

# Core dependencies
PyYAML>=6.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional but recommended
jupyter>=1.0.0
ipykernel>=6.0.0

# Note: neckenml should be installed separately
# Install with: pip install -e /path/to/neckenml
